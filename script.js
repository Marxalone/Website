// Optional: Add interactivity or additional features with JavaScript
document.addEventListener('DOMContentLoaded', () => {
  console.log("Yaremi Lash Loft is ready!");
  // Add any interactive features here, if necessary.
});